# Isotope Theme - Begin
# Alacritty Dark Medium Variant
# {{ author }} ({{ githubOrg }}/alacritty)
colors:
  primary:
    background: '{{ hex base00 }}'
    foreground: '{{ hex base09 }}'
  cursor:
    background: '{{ hex base02 }}'
    cursor:     '{{ hex base09 }}'
  selection:
    background: '{{ hex base03 }}'
    text:       '{{ hex base09 }}'
  normal:
    black:   '{{ hex base01 }}'
    red:     '{{ hex base20 }}'
    green:   '{{ hex base21 }}'
    yellow:  '{{ hex base22 }}'
    blue:    '{{ hex base23 }}'
    magenta: '{{ hex base24 }}'
    cyan:    '{{ hex base25 }}'
    white:   '{{ hex base06 }}'
  bright:
    black:   '{{ hex base03 }}'
    red:     '{{ hex base30 }}'
    green:   '{{ hex base31 }}'
    yellow:  '{{ hex base32 }}'
    blue:    '{{ hex base33 }}'
    magenta: '{{ hex base34 }}'
    cyan:    '{{ hex base35 }}'
    white:   '{{ hex base08 }}'
# Isotope Theme - End
