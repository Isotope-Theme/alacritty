# Isotope Theme - alacritty color config
# {{ author }} ({{ githubOrg }}/alacritty)
colors:
  primary:
    background: '{{ hex base09 }}'
    foreground: '{{ hex base00 }}'
  cursor:
    background: '{{ hex base08 }}'
    cursor:     '{{ hex base00 }}'
  selection:
    background: '{{ hex base07 }}'
    text:       '{{ hex base00 }}'
  normal:
    black:   '{{ hex base01 }}'
    red:     '{{ hex base0A }}'
    green:   '{{ hex base0B }}'
    yellow:  '{{ hex base0C }}'
    blue:    '{{ hex base0D }}'
    magenta: '{{ hex base0E }}'
    cyan:    '{{ hex base0F }}'
    white:   '{{ hex base06 }}'
  bright:
    black:   '{{ hex base03 }}'
    red:     '{{ hex base10 }}'
    green:   '{{ hex base11 }}'
    yellow:  '{{ hex base12 }}'
    blue:    '{{ hex base13 }}'
    magenta: '{{ hex base14 }}'
    cyan:    '{{ hex base15 }}'
    white:   '{{ hex base08 }}'
# Isotope Theme - End
