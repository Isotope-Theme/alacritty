# Isotope Theme - Begin
# Alacritty Light Soft Variant
# {{ author }} ({{ githubOrg }}/alacritty)
colors:
  primary:
    background: '{{ hex base09 }}'
    foreground: '{{ hex base00 }}'
  cursor:
    background: '{{ hex base07 }}'
    cursor:     '{{ hex base00 }}'
  selection:
    background: '{{ hex base06 }}'
    text:       '{{ hex base00 }}'
  normal:
    black:   '{{ hex base01 }}'
    red:     '{{ hex base30 }}'
    green:   '{{ hex base31 }}'
    yellow:  '{{ hex base32 }}'
    blue:    '{{ hex base33 }}'
    magenta: '{{ hex base34 }}'
    cyan:    '{{ hex base35 }}'
    white:   '{{ hex base06 }}'
  bright:
    black:   '{{ hex base03 }}'
    red:     '{{ hex base20 }}'
    green:   '{{ hex base21 }}'
    yellow:  '{{ hex base22 }}'
    blue:    '{{ hex base23 }}'
    magenta: '{{ hex base24 }}'
    cyan:    '{{ hex base25 }}'
    white:   '{{ hex base08 }}'
# Isotope Theme - End
